app = "telegram-shift-bot"
primary_region = "fra"

[build]

[env]
TIMEZONE = "Europe/Kyiv"

[processes]
app = "python -m app.main"

[http_service]
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1
